import { LocalAuthExceptionFilter } from './local-auth-exception.filter';

describe('LocalAuthExceptionFilter', () => {
  it('should be defined', () => {
    expect(new LocalAuthExceptionFilter()).toBeDefined();
  });
});
